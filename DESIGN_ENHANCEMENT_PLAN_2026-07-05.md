# デザイン強化工程書 2026-07-05
## 「学びの地図」を、もっと魅力的で・リッチで・知的なサイトにする

この文書は、実装担当AI（Sonnet 4.6）への作業指示書である。
上から順にフェーズごとに実施し、各フェーズの完了条件を満たしてから次へ進むこと。

---

## 0. コンセプト

**「夜の天体図書館」— 星図のように美しく、書斎のように知的な場所。**

- トップ〜ハブページ群（index / note / podcast / infographics / evolution）は「夜の星図」。深い闇、静かな光、地図の手触り。
- 各部ページ（parts/）は「昼の書斎・和紙の本」。これは意図的なコントラストとして**維持**し、夜⇄昼の行き来を演出でつなぐ。
- リッチさは「派手さ」ではなく「密度と質感」で出す。余白、タイポグラフィ、微細な光、紙とインクの気配。
- 知的さは「装飾」ではなく「言葉の扱い」で出す。明朝体の問い、エピグラフ、静かな注釈。

## 1. 絶対に守ること（違反したら作業停止して報告）

1. **ダッシュボード的UIは禁止。** 進捗バー・パーセント表示・統計数値・達成バッジ・ランキングはLMS的で世界観を壊す。足あと（footprints.js）は無言・自動・控えめのまま。
2. **既存の黒基調・ネオンカラー・旅/地図/年表の世界観を維持**する（MANABIMAP_BASE_PLAN.md「守る前提」準拠）。
3. **既存ページ・既存機能を壊さない。** 特に:
   - `data-track-*` 属性（cta-tracking.js の計測）を1つも消さない・変えない
   - `manabimap-data.js` / `learning-curriculums.json` のデータ構造を変えない
   - footprints.js / sky-clock.js / manabi-search.js / part-connector.js の挙動を変えない
   - 各HTMLのid・アンカーリンクを維持する
4. **媒体アクセント色の意味を維持**する: 地図/YouTube=シアン `#00d4ff`、Podcast=橙 `#ffb86b`、図解=紫 `#8da2ff`、note=緑 `#41c9b4`、動画=赤 `#ff4d67`。
5. **フレームワーク・ビルドツールを導入しない。** 素のHTML/CSS/JSのまま。外部ライブラリ追加も原則禁止（Google Fonts追加のみ可）。
6. 1フェーズ = 1コミット以上の粒度でこまめにコミットする。コミットは日本語または英語の簡潔なメッセージ。**push はユーザーの指示があるまでしない。**

## 2. 現状の把握（実装前に必ず読むファイル）

| 目的 | ファイル |
|---|---|
| 世界観と前提 | `MANABIMAP_BASE_PLAN.md`（冒頭〜「現状の課題」まで） |
| トップの構造 | `index.html` の `<nav>` 以降（約2878行目〜） |
| 共通CSS変数の現状 | 各HTMLの `<style>` 冒頭の `:root`（ページごとに微妙に不揃い） |
| 部ページの和紙テーマ | `parts/part9.html` の `:root`（`--paper: #faf8f2` 系） |

**注意:** HTMLは1ページ3000〜4000行あるため全読み込みしない。`grep -n` で該当箇所を特定し、範囲Readで作業すること。

---

## Phase 1: デザイントークンの整備（基盤） ✅ 完了 (2026-07-05)

**目的:** ページごとにバラついたCSS変数を共通トークンに集約し、以降のフェーズを一括適用可能にする。

### 作業
1. `assets/css/tokens.css` を新規作成。内容:
   - 背景系: `--bg: #08080d` / `--bg-card: #10101a` / `--bg-card-hover: #18182a` / `--border: #2a2a40`
   - 文字系: `--text: #e8e8f0` / `--text-dim: #a3a3bf` / `--text-bright: #ffffff`（note.htmlの `#e0e0e8`/`#9b9bb4` はこの値に寄せる）
   - 媒体色: `--map` / `--podcast` / `--zukai` / `--note-green` / `--youtube-red`（上記1-4の値）
   - 新規追加トークン: 影 `--shadow-soft` / `--shadow-glow`、角丸 `--radius-card: 14px` 等、字間 `--tracking-wide: 0.08em`、遷移 `--ease-out: cubic-bezier(0.22,1,0.36,1)`
2. index / note / podcast / infographics / evolution の5ページに `<link rel="stylesheet" href="assets/css/tokens.css">`（parts配下は相対パス調整）を追加し、各ページの `:root` から重複定義を削除。**ページ固有の変数（evolutionのneon系、partsの和紙系）はそのまま残す。**
3. evolution.html の背景 `#05050c`/`#02020a` は演出として維持してよいが、tokens.css に `--bg-deep` として収録する。

### 完了条件
- 5ページすべてで見た目の変化がない（リグレッションゼロ）ことをブラウザで目視確認。
- 共通変数の二重定義が残っていない。

---

## Phase 2: タイポグラフィの知性化 ✅ 完了 (2026-07-05)

**目的:** 「知的な感じ」の核。書体の格を上げ、日本語の組版を丁寧にする。

### 作業
1. **明朝体の導入:** Google Fonts から `Shippori Mincho`（しっぽり明朝、weight 500/700）を追加。用途を限定する:
   - ページ内の「問い」・引用・エピグラフ・セクションのリード文のみ
   - 本文やUIには使わない（本文は Noto Sans JP のまま）
2. **見出しの統一:** 英字ディスプレイは Cinzel に統一（index / evolution で既に使用。podcast / note / infographics のセクション見出しの英字ラベルにも適用）。
3. **組版の微調整**（tokens.css にユーティリティとして定義）:
   - 見出し: `letter-spacing: 0.06〜0.12em`、`font-feature-settings: "palt"` で日本語詰め
   - 本文: `line-height: 1.9`、`text-wrap: pretty`（対応ブラウザのみ効く。フォールバック不要）
   - 数字・年号: `font-variant-numeric: tabular-nums`（evolution年表で特に効く）
4. **階層の明確化:** 各ハブページのカードで「英字ラベル（Cinzel・小・字間広め）→ 日本語タイトル（太）→ 説明（dim）」の3層リズムを揃える。

### 完了条件
- 明朝体が「問い・引用」だけに使われている（乱用していない）。
- フォント読み込みは `display=swap` を維持し、既存の Noto Sans JP のリクエストと統合されている（`<link>` が増えすぎていない）。

---

## Phase 3: 質感と深度（リッチさの核） ✅ 完了 (2026-07-05)

実施メモ:
- ヘッダー/ナビのガラス化（項目4）は index/note/podcast/infographics/evolution 全ページで既に実装済みだったため変更なし。
- evolution.html の時代区切りは既存の `.era-header::before` の光る点がすでに「経緯線＋点」の役割を果たしていたため、`.event-date`/`.era-range` のトーンを揃えるのみで対応。
- 和紙テーマとの橋渡し（項目5）は parts/part9.html にのみ適用（Phase 6 のロールアウト方針に従い、残り12ファイルへの展開はユーザー確認後）。

**目的:** フラットな黒背景に「夜の天体図書館」の空気を足す。すべて控えめに。

### 作業
1. **粒子ノイズテクスチャ:** CSSのみで極薄のフィルムグレイン（SVG feTurbulence をdata URIで `opacity: 0.02〜0.03`、`pointer-events: none` の固定オーバーレイ）。5ハブページに適用。
2. **カードの深度:** ホバー時に「持ち上がる」のではなく「灯りがともる」方向で統一:
   - `box-shadow` にアクセント色の淡いグロー（`--shadow-glow`、媒体色を `color-mix()` で10〜15%に希釈）
   - ボーダーが媒体色に静かに変化（transition 0.4s、`--ease-out`）
   - `transform` は `translateY(-2px)` まで。それ以上動かさない
3. **星図の罫線:** セクション区切りを単なる `<hr>` 的な線ではなく、「経緯線」風の極細線＋小さな十字/点のSVG装飾に。index.html のセクション間に2〜3箇所、evolution.html の時代区切りに適用。
4. **ヘッダー/ナビのガラス化:** sticky navに `backdrop-filter: blur(12px)` + 半透明背景。既に近い実装があれば質感を揃えるだけでよい。
5. **和紙テーマとの橋渡し:** 部ページ（parts/）側は色を変えず、紙の質感（同じfeTurbulence手法で暖色系の極薄グレイン）だけ足して「夜の星図 ⇄ 昼の和紙」の質感の対を作る。

### 完了条件
- すべての効果が「言われないと気づかない」レベルの控えめさ（opacity・彩度を疑ったら下げる方向で調整）。
- スクロールが60fpsを保つ（`will-change` 乱用禁止、blurはnavのみ）。
- ダッシュボード的要素が一切増えていない。

---

## Phase 4: モーション（静かな生命感） ✅ 完了 (2026-07-05)

実施メモ:
- index.html / evolution.html には同等のスクロールリビール（reveal-init/reveal-on、reduced-motion対応込み）が既に実装済みだったため、reveal.js は podcast / note / infographics の3ページにのみ導入。クラス命名は既存実装に合わせた。
- ヒーローの星の瞬き（項目3）は既存の `--d` によるディレイ分散で十分に機械感が消えていたため変更なし。
- View Transitions は tokens.css 経由で全読み込みページ（5ハブ＋part9）に適用。

**目的:** ページが「生きている」感覚。ただし星空の速度で。

### 作業
1. **スクロールリビール:** IntersectionObserver で、セクション・カードが `opacity 0→1` + `translateY(12px→0)`（duration 0.8s、stagger 60ms）。共通スクリプト `assets/js/reveal.js` を新規作成し5ハブページに導入。
2. **`prefers-reduced-motion: reduce` 対応必須:** メディアクエリで全アニメーションを無効化（reveal.js 側でも判定して即時表示）。
3. **ヒーローの星の瞬き:** index.html に既存の star アニメーションがあれば周期をランダム化して機械感を消す。なければ追加しない。
4. **ページ間トランジション:** View Transitions API（`@view-transition { navigation: auto; }`）でクロスフェード0.3s。非対応ブラウザでは何も起きない、で良い。

### 完了条件
- reduced-motion 環境で全アニメーションが止まる。
- アニメーションの重複発火・スクロールのカクつきがない。
- 「動きが目立つ」と感じたら遅く・小さくする方向で調整済み。

---

## Phase 5: 知的装置（コンテンツの見せ方） ✅ 完了 (2026-07-05)

実施メモ:
- 今日の問い: manabimap-data.js の各部 `questions` フィールド（全19問）から日付シードで決定的に選択。index.html ヒーロー直下に明朝体＋出典部リンク付きで表示。
- エピグラフ起草文（差し替え可能）: podcast「答えより先に、問いを聴く。」/ note「短い読み物が、長い問いになる。」/ 図解の間「一枚の絵は、千の言葉より一つの問いを残す。」
- evolution.html は既存の `.hero-tagline`（138億年前…）が既にエピグラフの役割を果たしているため追加せず。
- 星座線: part-connector.css の「問い」ブロックを1px線＋端点の光る点＋明朝体に、ルートステップ間に接続線を追加（モバイル縦積みでは非表示）。機能・データは不変。

**目的:** 装飾ではなく「言葉」でサイトの知性を上げる。**新規コンテンツの執筆は最小限**（既存データから引く）。

### 作業
1. **今日の問い:** index.html のヒーロー直下に、`manabimap-data.js` 内の各話の「問い」テキストから日替わりで1つ表示する静かな一節（明朝体・小さめ・出典部リンク付き）。日付シードで決定的に選ぶ（ランダムでなく、同じ日は同じ問い）。データ内に問いに相当するフィールドがあるか先に確認し、なければこの項目はスキップして報告。
2. **エピグラフ:** 各ハブページ（note / podcast / infographics / evolution）のタイトル下に、そのページの性格を表す一文を明朝体で置く（例: 図解の間「一枚の絵は、千の言葉より一つの問いを残す」）。文言は4ページ分この工程書の実装者が起草し、コミットメッセージに明記してユーザーが後で差し替えられるようにする。
3. **関連の糸:** 各部ページ下部の part-connector 表示部の見た目を「星座の線」風（細線＋端点の点）に整える。**機能・データは変えない。見た目だけ。**
4. **脚注スタイル:** note.html のカード説明文などで使える控えめな補足スタイル `.aside-note`（左に極細のアクセント線、明朝体、dim色）を tokens.css に定義。適用は無理にしない（今後の記事用の器）。

### 完了条件
- 追加された言葉がすべて「問い・誘い」のトーンで、宣伝調・説明調になっていない。
- 統計・数値・達成表示が紛れ込んでいない。

---

## Phase 6: ページ個別の磨き込み ✅ 完了 (2026-07-05)

実施メモ:
- index: CTA星を10→7個に削減し、軌道座標を再調整して非対称に。入口カードの灯りホバーは Phase 3 で適用済み。
- note: `6n+3` のカードを `grid-column: span 2`＋21:9の横長画像＋タイトル微拡大で「棚の中の大きな一冊」に。560px以下では従来どおり1カラムに戻る。
- evolution: era-number のフォント参照を `var(--font-display)` に統一（見た目は従来どおり）。tabular-nums / Cinzel は Phase 2 で適用済み。
- 部ページ展開: ユーザーから「Phase 6まで続行」の指示を受けたため、part9 の確認待ちを経ずに残り12ファイル（part1〜8, 8_2, 9_1〜9_3）へ同一パッチ（tokens.css リンク＋暖色グレイン）を一括適用。全ファイル同一の2行diff であることを確認済み。
- モバイル375px: index / note / part1 で横スクロールなし・崩れなしを確認。

**目的:** 各ページ固有の弱点を1点ずつ解消する。

### 作業
1. **index.html:** ヒーローのCTA星（`hero-cta-star` ×10個）が装飾過多なら数を減らし配置を非対称に。「目次」感を減らすため、入口カード（mock-entrances）に Phase 3 の灯りホバーを最優先で適用。
2. **note.html:** カードグリッドの単調さを、`nth-child` ベースのわずかなサイズ変化（既存の `6n+3` 拡大の延長）で「図書館の棚」的リズムに。130本超あるため、レンダリング負荷を悪化させない。
3. **evolution.html:** 年表の時代見出しに `tabular-nums` と Cinzel を適用。ネオン色は現状維持。
4. **podcast.html / infographics.html:** Phase 2〜4 の共通適用のみで良い。個別作業は最小限。
5. **parts/part1〜9:** 一括で同じ変更を当てるため、まず part9.html で完成させてユーザー確認を待ち、承認後に残り12ファイルへ同一パッチを展開する。

### 完了条件
- 全13部ページが同じ質感になっている（diffで差分が意図通り同一）。
- モバイル幅（375px）で全ページ崩れなし。

---

## 検証手順（各フェーズ共通）

1. ローカルで `python3 -m http.server` 等で配信し、Chrome で確認（`file://` では fetch 系が動かないため）。
2. チェック項目:
   - [ ] 対象ページの表示崩れなし（デスクトップ1280px / モバイル375px）
   - [ ] コンソールエラーゼロ
   - [ ] `data-track-*` 属性の数が作業前後で一致（`grep -o 'data-track-event' *.html parts/*.html | wc -l` で前後比較）
   - [ ] reduced-motion でアニメーション停止（Phase 4以降）
3. スクリーンショットを撮ってフェーズ完了報告に添付する。

## 進め方のルール

- フェーズ順は 1→2→3→4→5→6。スキップ・順序変更をする場合は理由を報告してから。
- 判断に迷う視覚的トレードオフ（色の濃さ、動きの量）は「控えめな方」を選ぶ。これがこのサイトの美学である。
- この工程書と実装が食い違う事態が生じたら、**先にこの文書を更新してから**実装する（MANABIMAP_BASE_PLAN.md と同じ運用）。
- 完了したフェーズはこの文書の該当見出しに `✅ 完了 (日付)` を追記する。

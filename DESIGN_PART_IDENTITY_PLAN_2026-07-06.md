# 部アイデンティティ設計工程書 2026-07-06
## トップページの各部を「色が違うだけのカード」から「固有の顔を持つ章」へ

この文書は、実装担当AI（Sonnet）への作業指示書である。
前回工程書 `DESIGN_ENHANCEMENT_PLAN_2026-07-05.md`（全Phase完了済み）の続編にあたる。
上から順にフェーズごとに実施し、各フェーズの完了条件を満たしてから次へ進むこと。

---

## 0. コンセプト

**「十の星座を持つ天体図」— 部はカードではなく、固有の紋章と星座を持つ章である。**

前回工程で「夜の天体図書館」の世界観と、部の連続色スペクトル（檜皮→藤紫、`tokens.css` の `--part1`〜`--part9`・`--part8-2`）は完成した。
しかし現状のトップページでは、各部は**色相が違うだけの同一形状カード**であり、10の部を並べたとき「スペクトルの美しさ」はあっても「章ごとの顔」がない。

この工程書の目的は、**統一されたシステムの中で、各部に固有の視覚的アイデンティティを与える**こと。
方針は3層構造：

| 層 | 全部で共通（システム） | 部ごとに固有（アイデンティティ） |
|---|---|---|
| 色 | スペクトル生成規則 hue≈24+(n-1)×34 | 和色名（檜皮・琥珀・柳…）を**表に出す** |
| 形 | 線画SVGの作法（1.5px線・単色・currentColor） | 部の紋章（シジル）1点 |
| 質感 | 星座パターンの描画作法 | 部ごとの星座配置（シード値で固有化） |

「派手さ」ではなく「密度と質感」でリッチさを出す前回方針を継承する。紋章も星座も**静かで小さい**こと。

## 1. 絶対に守ること（違反したら作業停止して報告）

前回工程書の制約をすべて引き継ぐ。特に：

1. **ダッシュボード的UIは禁止。** 進捗バー・統計数値・達成バッジ・ランキングを追加しない。
2. **既存機能を壊さない：**
   - `data-track-*` 属性を1つも消さない・変えない（作業前後で `grep -o 'data-track-event' *.html parts/*.html | wc -l` が一致すること）
   - `manabimap-data.js` の既存フィールドの意味・構造を変えない（**フィールド追加は可**）
   - footprints.js / sky-clock.js / manabi-search.js / part-connector.js の挙動を変えない
   - 各HTMLのid・アンカーリンクを維持する
3. **媒体アクセント色の意味を維持**：地図/YouTube=シアン `#00d4ff`、Podcast=橙 `#ffb86b`、図解=紫 `#8da2ff`、note=緑 `#41c9b4`、動画=赤 `#ff4d67`。部の色と媒体の色を混同しない。
4. **フレームワーク・外部ライブラリ・画像生成に頼らない。** 紋章はインラインSVG（手書きの線画コード）で作る。PNGアイコンを新規追加しない。
5. **部スペクトルの生成規則を壊さない。** 新しい部が増えても hue 規則で継げる設計を維持する。
6. 1フェーズ = 1コミット以上。**push はユーザーの指示があるまでしない。**
7. 判断に迷う視覚的トレードオフは「控えめな方」を選ぶ。これがこのサイトの美学である。

## 2. 現状の把握（実装前に必ず読む箇所）

HTMLは3000〜4000行あるため**全読み込みしない**。`grep -n` で特定し範囲Readすること。

| 目的 | 場所 |
|---|---|
| 部の色トークンと和色名 | `assets/css/tokens.css` 35〜44行付近（`--part1`〜`--part9`、コメントに和色名） |
| 部カードのCSS | `index.html` 985〜1310行付近（`.atlas-map` `.map-node` `.map-node-detail`） |
| 部カードの生成JS | `index.html` 3730〜3760行付近（`atlasNodes.forEach` → `button.innerHTML`） |
| 部データ | `assets/js/manabimap-data.js` の `parts` 配列（id/number/title/subtitle/group/era/color/questions/tags…） |
| 部表紙の共通CSS | `assets/css/part-hero.css`（前回Phase 7で全部ページに適用済み） |
| 前回工程の全記録 | `DESIGN_ENHANCEMENT_PLAN_2026-07-05.md` |

---

## Phase 1: 和色名を部のアイデンティティとして表に出す ✅ 完了 (2026-07-06)

**目的:** すでに `tokens.css` のコメントにしか存在しない和色名（檜皮・琥珀・柳・常磐・青磁・浅葱・縹・菫・紫苑・藤紫）を、ユーザーの目に触れる「部の名前の一部」に昇格させる。コストが最小で知的効果が最大の一手。

### 作業
1. `manabimap-data.js` の各部オブジェクトにフィールドを追加する：
   ```js
   colorName: '縹',        // 和色名（漢字）
   colorReading: 'はなだ', // 読み
   ```
   対応表（tokens.css のコメントと一致させること）：
   part1=檜皮(ひわだ) / part2=琥珀(こはく) / part3=柳(やなぎ) / part4=常磐(ときわ) / part5=青磁(せいじ) / part6=浅葱(あさぎ) / part7=縹(はなだ) / part8=菫(すみれ) / part8-2=紫苑(しおん) / part9=藤紫(ふじむらさき)
2. トップの部カード（`.map-node`）の `map-node-num` 行を拡張：
   `PART 07` の右側に、部色の小さな和色名を添える。例：`PART 07 ── 縹`。
   フォントは明朝（既存の見出し用フォント変数を使う）、10px、letter-spacing広め、透明度0.75程度。
3. 詳細パネル（`.map-node-detail`）にも同じ和色名行を入れる。ここでは読みも添える：`縹 ── はなだ`。
4. 各部ページの表紙（`part-hero.css` を使う `.cover`）のシリーズ行付近にも同じ和色名を1箇所だけ入れる。目立たせない。**部ページ13枚すべてで確認する。**

### 完了条件
- [ ] 10部すべてのカード・詳細パネル・部表紙に和色名が表示される
- [ ] 和色名の色がその部の `--partN` と一致している
- [ ] モバイル375pxで折り返し崩れなし

---

## Phase 2: 部の紋章（シジル）の設計と実装 ✅ 完了 (2026-07-06)

**目的:** 各部に固有の線画紋章を与える。星図の記号のような、単純な幾何学線画。カード・詳細パネル・部表紙で一貫して使い、「この形＝この部」という認識を作る。

### 紋章の作法（全部共通）
- インラインSVG、viewBox="0 0 48 48"、`stroke="currentColor"`・`fill="none"`・`stroke-width="1.5"`・`stroke-linecap="round"`
- 要素数は**5ストローク以内**。複雑にしない。天体記号・錬金術記号・星図の約物のような佇まい
- 色は置かない（currentColor で部色を継承させる）
- JSから使えるよう、`assets/js/part-sigils.js` に `PART_SIGILS = { part1: '<svg…>', … }` として定義し、`window.PART_SIGILS` で公開する

### 各部の紋章モチーフ（この意図で線画を設計すること）
| 部 | テーマ | 紋章モチーフ |
|---|---|---|
| part1 | 蒸気の時代 | 円＋上昇する3本の波線（釜と蒸気） |
| part2 | つながる世界 | 3点を結ぶ弧（電信の弧、点と線） |
| part3 | AIと生きる未来 | 二重丸の交差（人と機械の重なり） |
| part4 | 文字と知識の冒険 | 開いた頁を示す2枚の斜線面（写本） |
| part5 | 言葉と思考の旅 | 渦巻き1本（内へ向かう思考） |
| part6 | 数字と世界 | 目盛りつき水平線＋1点（測る） |
| part7 | 人間の前提を外す | 正方形の枠から1辺が外れて開く（前提が外れる） |
| part8 | AIと人間のあいだ | 接する2つの円（あいだ） |
| part8-2 | AIが日常に入る日 | 家形の五角形＋内側の小円（日常に灯る） |
| part9 | 身体という時間 | 円と縦の正中線＋左右の弧（身体の軸と呼吸） |

デザインの最終判断は実装者に委ねるが、**モチーフの意味は上表から変えない**。迷ったらより単純な形を選ぶ。

### 配置
1. トップの部カード：現在 `.map-node::before` で描いている菱形の光点を、**部の紋章に置き換える**（サイズ18px程度、光のにじみ box-shadow は現状の階調を維持）。evolution ノードなど部以外のノードは菱形のまま。
2. 詳細パネル：見出し脇に24px で表示。
3. 部表紙（`.cover`）：問いの上か経緯線 divider の中央に、28px・透明度0.5程度で静かに置く。
4. 紋章にはアニメーションを付けない。hover時にカード全体が光る既存挙動だけで十分。

### 完了条件
- [ ] 10部すべてに固有紋章が表示され、カード/詳細/表紙で同じ形が使われている
- [ ] SVGはすべて currentColor 継承で、部色変更に自動追従する
- [ ] reduced-motion 環境で問題なし（紋章は無アニメなので自然に満たされるはず）

---

## Phase 3: 部ごとの固有星座パターン ✅ 完了 (2026-07-06)

**目的:** カード背景に、部ごとに配置の異なる微細な星座（点＋細線）を敷く。「同じ夜空だが、章ごとに見えている星座が違う」という質感を作る。

### 作業
1. `assets/js/part-sigils.js` に星座生成関数を追加する：
   ```js
   function constellationSvg(seed, color) { … }
   ```
   - 部idの文字列から決定的なシード値を作り（ハッシュでよい）、5〜7個の点をカード領域に散らし、そのうち3〜4点を細線（stroke-width 0.75、透明度0.3）で結ぶ
   - **乱数を毎回引かない**こと。同じ部は再読み込みしても常に同じ星座
   - 出力は `<svg>` 文字列。カードの `::after` ではなくカード内の絶対配置要素（`.map-node-constellation`、pointer-events:none、z-index はテキストより下）として挿入する
2. 点の透明度は0.35〜0.6でランダム、最大でも1.5px半径。**主張しないこと。**カードを3秒見た人だけが気づく密度が正解。
3. hover時に星座の透明度をわずかに上げる（0.3→0.5程度、transition 0.4s）。それ以上の動きは付けない。
4. 詳細パネルの背景にも同じ部の星座を、より大きく淡く（透明度半分）敷く。

### 完了条件
- [ ] 各部のカードに固有の星座が出る。リロードしても同じ配置
- [ ] テキストの可読性が落ちていない（星座が文字に重なっても読める淡さ）
- [ ] パフォーマンス劣化なし（SVGはDOM挿入1回のみ、アニメーションループを追加しない）

---

## Phase 4: カードの構造的差異 — 「群」の視覚化 ✅ 完了 (2026-07-06)

**目的:** 色と紋章で「部の個性」を出した上で、部が属する**群**（`group` フィールド：技術の歴史／知の歴史／前提を超える…等）のまとまりを静かに感じさせる。現在 `.map-node-group` は `display:none` で殺されている。

### 作業
1. まず `manabimap-data.js` の `group` 値を全部分について棚卸しし、群の一覧をこの工程書に追記する（実装前に群がいくつあるか確認）。
   - 棚卸し結果（2026-07-06）：
     - `三大革命`：part1 / part2 / part3
     - `文明の道具`：part4 / part5 / part6
     - `前提を超える`：part7
     - `いま、ここ`：part8 / part8-2
     - `身体と進化`：part9
2. `.map-node-group` を復活させるのではなく、**カード上辺の細いルール線**として群を表現する：
   - 同じ群の部は、カード上辺に同じスタイルの1px線を持つ（例：実線／破線／二重線／点線を群ごとに割り当て）
   - 色はその部の部色のまま。**群に新しい色を導入しない**（色は部、線種は群、という役割分担）
3. 詳細パネルには群名をテキストで小さく表示してよい（「知の歴史」等、10px・dim色）。
4. グリッドの並び順は変更しない（number順を維持）。並び替えやグルーピング見出しの挿入は**しない**——地図の一覧性を壊すため。

### 完了条件
- [ ] 同じ群のカードを見比べると上辺の線種が揃っていると分かる
- [ ] 初見では気づかない程度に控えめ（線の透明度0.5以下）
- [ ] 並び順・グリッドレイアウトは作業前と同一

---

## Phase 5: 詳細パネルの「章扉」化 ✅ 完了 (2026-07-06)

**目的:** カードをクリック/選択したときの `.map-node-detail` を、単なる情報パネルから「その部の章扉」に格上げする。部のアイデンティティ要素（和色名・紋章・星座・問い）がここに集約される。

### 作業
1. 詳細パネルの構成を次の順に整える（既存の情報要素は削除しない。順序と扱いを変える）：
   - 上段：紋章（24px）＋ `PART 07` ＋ 和色名（読み付き）
   - 中段：タイトル（明朝・大きく）＋ subtitle
   - 問い：`questions[0]` を明朝のエピグラフとして1行（既存の問い表示があれば体裁だけ整える）
   - 下段：既存のリンク群・メディア（`data-track-*` は完全維持）
2. パネル背景に Phase 3 の星座を淡く敷く。
3. パネル切替時のトランジションは opacity 0.25s のフェードのみ。スライドや拡大は付けない。

### 完了条件
- [ ] 部を切り替えると、色・和色名・紋章・星座・問いが一式で切り替わる
- [ ] `data-track-*` 属性数が作業前後で一致
- [ ] キーボード操作・スクリーンリーダーで到達可能（既存のbutton構造を壊していない）

---

## Phase 6: 部ページ表紙への波及（トップとの往復一貫性） ✅ 完了 (2026-07-06)

**目的:** トップで確立した部アイデンティティ（和色名・紋章・星座）を部ページ表紙にも反映し、「トップで見た章の顔」が部ページでも迎えてくれるようにする。

### 作業
1. `part-hero.css` に紋章・和色名・星座のスタイルを追加する（Phase 1-3, 2-3 で個別に入れたものをここで共通化・整理する）。
2. 全13部ページ（part1〜part9、part8-2、partN_1/2/3 系の分割ページがあれば `ls parts/` で確認）に適用。分割ページ（partN_1.html 等）は親部と同じ紋章・和色名を使う。
3. 星座は表紙の夜空領域にのみ敷く。本文（和紙領域）には入れない——夜と昼のコントラストは前回工程で意図的に維持した設計である。

### 完了条件
- [ ] トップのカード → 部ページ表紙で、同じ紋章・同じ和色名が確認できる
- [ ] 和紙領域（本文）には夜の要素が漏れていない
- [ ] 全部ページでコンソールエラーゼロ

---

## 検証手順（各フェーズ共通）

1. ローカルで `python3 -m http.server` 等で配信し、Chromeで確認（`file://` では fetch 系が動かない）。
2. チェック項目：
   - [ ] デスクトップ1280px / モバイル375px で表示崩れなし
   - [ ] コンソールエラーゼロ
   - [ ] `grep -o 'data-track-event' *.html parts/*.html | wc -l` が作業前後で一致
   - [ ] prefers-reduced-motion でアニメーション停止
3. スクリーンショットを撮ってフェーズ完了報告に添付する。

## 進め方のルール

- フェーズ順は 1→2→3→4→5→6。Phase 1〜2 が本体、3〜4 は質感、5〜6 は波及。時間対効果を優先するなら 1→2→5 を先に完了させ、3→4→6 を後回しにしてよい（その場合は理由を報告してから）。
- 迷ったら控えめな方。紋章と星座は「気づいた人へのご褒美」であり、初見の主張ではない。
- この工程書と実装が食い違う事態が生じたら、**先にこの文書を更新してから**実装する。
- 完了したフェーズはこの文書の該当見出しに `✅ 完了 (日付)` を追記する。
